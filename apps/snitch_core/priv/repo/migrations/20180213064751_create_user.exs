defmodule Core.Repo.Migrations.CreateUser do
  use Ecto.Migration

  def change do
    create table(:snitch_users) do
      # Just a name perhaps?
      add :first_name, :string
      add :last_name, :string
      add :email, :string
      add :password_hash, :string, comment: "Generated by
          argon2_elixir:hash_pwd_salt with default 16 byte salt length."
      add :is_admin?, :boolean, default: false

      add :sign_in_count, :integer, default: 0
      add :failed_attempts, :integer, default: 0
      timestamps()
    end

    create unique_index(:snitch_users, [:email])
  end
end
